<h2>Task</h2>
<%= link_to 'Edit Task', edit_task_path %>
<p><%= @task.title %></p>
<p><%= @task.details %></p>
<%= link_to 'Delete Task', task_path(@task), data: {turbo_method: :delete, turbo_confirm: 'Are you sure you want to delete the task?'} %>
<br>
<br>
<%= link_to 'Back to list', tasks_path %>
